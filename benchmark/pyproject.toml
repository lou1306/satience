[project]
name = "satience-benchmark"
version = "0.1.0"
description = "Benchmark satience SAT solver using gbd-tools"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gbd-tools>=5.0.0",
    "polars>=1.41.2",
]

[dependency-groups]
dev = []

[project.scripts]
benchmark = "satience_benchmark:main"
