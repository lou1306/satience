# Project: satience - CDCL SAT Solver in Go

## Goal & Constraints
Build a sound and complete CDCL SAT solver in Go with DIMACS CNF support, benchmarking on real GBD instances.
- DIMACS format only, single-threaded, no incremental/parallel/proof
- **No cardinality constraint detection** (PHP instances out of scope)
- 60s timeout per benchmark, GOAMD64=v3 build
- SAT Competition 2026 output format (exit 10=SAT, 20=UNSAT, 0=UNKNOWN)

## Status
Sound and complete. 15/15 unit tests, 100% soundness on 2000+ fuzzer iterations.
MiniSat Fast Suite (30s timeout): **72/72 solved (100%)** (July 2026). Verified sound via minisat cross-check (`benchmark/cross_check_minisat.sh`): 0 mismatches.

### Recent Fixes (July 2026)
- **BVE default budget + Glucose ratio tuning + Luby base increase**: Three changes that together took the suite from 66/72 to 72/72 (100%). (1) BVE default `veBudget` was 0 (unlimited) for instances below the 50K-var/500K-clause threshold. Instance 919d8fbf (23609v, 472858c) — just under the clause threshold — had BVE generate 22M resolvents, taking 31s (the entire timeout). Set default `veBudget=5000000` (5M) for all instances; large instances still override to 2M. 919d8fbf: 31s→8.6s (TIMEOUT→SAT@9.3s). (2) Glucose restart ratio 1.5→10.0: the Glucose EMA criterion was firing too often, causing unproductive restarts that disrupted deep search. Ratio 10.0 effectively disables it for most instances while keeping it as a safety net. Gained 69d72f81 (TIMEOUT→UNSAT@28s), de2b584e (TIMEOUT→SAT@14s). (3) Luby restart base 100→200: fewer Luby fallback restarts let the search go deeper before restarting. PAR2: 6.29s → 4.12s. Soundness verified: cross-check 0 mismatches (0 timeouts).
- **Blit encoding: litTrue index**: Changed Blit from Literal encoding (bit 31=negated) to litTrue index (varIdx*2 + negated) via `litToBlit()` helper. The fast-path check changed from `litValue[watch.Blit>>1]` (with bit manipulation) to `litValue[watch.Blit]` (direct index). Eliminates the `>>1` shift in the hottest path. Sentinel: Blit=0 means "none found" but is also valid for var 0 positive (litTrue index 0) — accepted as rare, worst case is missed Blit cache hit. No suite change (66/72). Soundness verified: cross-check 0 mismatches.
- **learnedAlive bitmap**: Added `[]byte` bitmap alongside `learnedLoc` for tombstone checking in the propagation hot path. `learnedAlive[learnedIdx] == 0` replaces `learnedLoc[learnedIdx].Size == 0` — 1-byte access vs 8-byte struct access, more cache-friendly. No suite change. Soundness verified: cross-check 0 mismatches.
- **watchLists cache + compaction fix**: (1) Cache `s.watchLists` outer slice as a local in `propagateWatched` (same pattern as `assignments`/`originalClauses`/`litValue`). The outer slice is allocated once in `initWatches` and never grows, so the cached header stays valid. Eliminates the `s → s.watchLists` pointer chase on every append (930ms) and write-back (370ms) in the hot loop. (2) Skip original-clause watch rebuild in `compactLearnedClauses`. Original clauses don't move during learned-clause compaction, so their watches (`ClauseIdx >= 0`) are still valid. Only learned-clause watches have stale indices. Now filters each watch list in-place to keep only original-clause watches, then re-adds compacted learned clauses. Eliminates the O(total original literals) `chooseWatchPositions` re-scan that the old full-rebuild did. 8d58ca18: SAT@33s → SAT@15.7s (2.1x faster). Soundness verified: fuzzer 406/406, cross-check 0 mismatches.
- **Lazy VSIDS heap + polarity-based initial phase**: (1) Lazy heap with sink approach: `bumpLarge`/`bumpLBD` don't call `increaseKey` (O(1) bump). `selectVariableWithHeap` peeks at root instead of `removeMax`; if assigned, sinks to -Inf (stays in heap, drops to bottom). `onUnassign` restores sunk entries' real scores and sifts up. `buildHeap` includes ALL variables (assigned at -Inf, unassigned at real score) — heap never empties, eliminating frequent O(n) rebuilds (was 15.6% of CPU on 822378be). Periodic refresh every 2000 conflicts fixes deeply stale entries. `restart()` forces a rebuild (sunk entries not restored by onUnassign). The eager-increaseKey variant was tested and rejected: O(log n) per bump on the full-N heap is more expensive than O(log k) on the smaller old heap (only unassigned vars). (2) Polarity-based initial phase: `savedPhase` initialized from occurrence counts (more frequent polarity wins) instead of defaulting all-true. +3 solved (62→65), -27% PAR2. Gained: 30eb4ef4, 8202af80, 66e6fea6, daf59d67. Lost: b8143c9d (trajectory shift from lazy heap staleness — inherent trade-off, not fixable without losing the gained instances). Soundness verified: fuzzer 406/406, cross-check 0 mismatches.
- **Recursive minimization fix: skip level-0 literals in reason clauses**: `exploreRemovable` returned false when a reason clause contained a level-0 literal, blocking minimization of the variable being explored. Level-0 literals are globally assigned (forced during preprocessing, never cleared by backjump), so they are permanently false and automatically "covered" — they should be skipped (`continue`), not cause failure. The old `return false` devastated long-clause instances: reason clauses averaging 50-150 literals almost always contain a level-0 literal, causing ~99% of minimization attempts to fail. On 822378be (86% long clauses), minimization succeeded only 0.9% (vs MiniSat's 16%). On b8143c9d (85% long), 0.3% vs 20%. Fix: `return false` → `continue` at the level-0 check. Matches Cadical/MiniSat (`if (!level(tmp)) continue;`). Soundness: the reason clause still propagates v after backjump because the level-0 literal remains false. b8143c9d: TIMEOUT→SAT@6.6s. 822378be: minimization 0.9%→14.3%, max clause 149→116, but still TIMEOUT (throughput-bound). Soundness verified: fuzzer 406/406, cross-check 0 mismatches.
- **Search quality fixes: classifier, clause DB sizing, minimization, restarts**: (1) Structure classifier: added `ternaryRatio` to size score (weight 0.5). Pure ternary instances were misclassified as random. (2) Clause DB floor: raised from 300 to `max(300, numVars*10)`. Small instances had maxLearned=300, retaining only 2-8% of learned clauses. (3) Minimize-depth CLI default: 100→0 (unlimited). The constructor set 0=unlimited but the CLI flag overrode it to 100. (4) Adaptive restart base for binary-heavy instances: if binaryRatio > 50%, set restartBase=20 (vs default 100). No suite change (66/72). PAR2: 7.06s → 7.01s. Soundness verified: fuzzer 300/300, cross-check 0 mismatches.
- **Classify-before-equiv + merge significance gate**: Two fixes to SCC-based equivalence detection. (1) `getAdaptivePreprocessingConfig()` (which computes the structure score AND sets restart/decay config) was running AFTER `detectEquivalences()`, classifying the post-equiv instance. Equiv merges variables and removes tautological clauses, changing binary/ternary/long ratios — causing misclassification. Moved classification BEFORE equiv so the structure score reflects the original instance (e.g., bb34f22f: 0.73 pre-equiv → structured config). (2) Equivalence detection merged even when the merge was insignificant (<1% of variables), changing the search trajectory for negligible reduction. Added a gate: if `mergedCount*100 < numVars`, skip the substitution (still detect l/¬l contradictions in SCC). daf59d67 (222406v, 200 merged = 0.09%): skip equiv → SAT@24.5s (was TIMEOUT). bb34f22f (7807v, 2790 merged = 35.7%): keep equiv. 65/72 → 66/72 (+1). Soundness verified: minisat cross-check 0 mismatches, fuzzer 300/300.
- **SCC-based equivalence detection**: Detects equivalent literals via Tarjan's SCC on the binary implication graph (built from binary clauses: (a∨b) gives edges ¬a→b, ¬b→a). Literals in the same SCC are equivalent; if l and ¬l are in the same SCC → UNSAT. Equivalent literals are merged into a representative, tautologies/duplicates from substitution are removed, and the mapping is stored for model reconstruction (`extendModel()` after SAT). Replaces the previously-banned pattern-matching variant that produced false equivalences. Significant merges on binary-heavy instances (e.g., bb34f22f: 2790 merged). Model verified on SAT instances with equivalences (262ba88b, 5a65b2816). Soundness verified: minisat cross-check 0 mismatches, fuzzer 400/400.
- **Usage-aware clause deletion + vivify LBD fix**: (1) Clause deletion was deleting clauses in arbitrary order within each LBD tier (LBD>5, then LBD>3). Now collects candidates per tier, sorts by PropCount ascending, and deletes least-propagated first — keeping high-traffic clauses that frequently cause propagations. The `PropCount` field was already maintained (incremented on each learned-clause propagation) but never read by the live deletion code. (2) Vivification shrinks clauses but never updated `learnedMetadata[].LBD`, leaving stale high-LBD values that caused valuable post-vivification glue clauses to be deleted. Fixed by setting `LBD = min(oldLBD, newSize)` (LBD ≤ clause size is a sound overestimate). No suite change (66/72) — the 6 remaining timeouts are throughput-bound (Go vs C) or search-quality-bound (deep search), not clause-database-quality-bound. Soundness verified: minisat cross-check 0 mismatches, fuzzer 400/400.
- **Size- and structure-adaptive LBD bonus scale**: The fixed `lbdBonusScale=10` (from the previous fix) caused bimodal behavior: small binary-heavy instances (e.g., `44092fcc`, 90v, 97.6% binary) need high LBD guidance (~2000) to escape bad trajectories, while long-clause instances (e.g., `274099073`, 80v, 99.4% long) need pure VSIDS (scale=10). A fixed scale solves one but not both. Replaced with adaptive formula: `scale = max(10, 200000 × binaryRatio / numVars)`. Binary-heavy small instances get ~2000, long-clause or large instances get 10. The formula uses binary clause ratio (not just size) because binary clauses produce LBD=2 glue clauses that benefit from guidance, while long clauses produce high-LBD clauses where the bonus dominates VSIDS. `44092fcc` TIMEOUT→UNSAT@4.5s, no regressions. 65/72 → 66/72 (+1). CLI: `-lbd-scale N` (non-zero overrides adaptive; default=0=adaptive). Soundness verified: minisat cross-check 0 mismatches, fuzzer 400/400.
- **LBD bonus scale reduction (2000→10)**: The LBD bonus (`lbdBonusScale=2000`) was 250x larger than VSIDS activity bumps (`baseBumpAmount=25`), making the heap score `activity + lbdBonus` dominated by LBD bonus. Variables in glue clauses (LBD=2: bonus=500) were permanently over-prioritized over variables with high VSIDS activity, causing the solver to repeatedly select the same variables → deep search (props/dec ~100, conflict level ~44-50) → wide 1-UIP clauses (62 vs minisat's 12 lits/clause pre-minimization). Reducing `lbdBonusScale` to 10 makes the glue-clause bonus (2.5) a tie-breaker (~10% of bump amount) instead of a dominator. 61/72 → 65/72 (+5 gained: `274099073` TIMEOUT→SAT, `8202af80` TIMEOUT→SAT, `8d58ca18` TIMEOUT→SAT, `93a5d44e` TIMEOUT→UNSAT, `b07759f4` TIMEOUT→UNSAT; -1 lost: `44092fcc` UNSAT@1s→TIMEOUT, small structured instance where LBD guidance mattered). Net +4. Soundness verified: minisat cross-check 0 mismatches, fuzzer 400/400.
- **Contiguous clause storage (single-storage)**: `Clauses[].Literals` previously pointed to per-clause allocations from the parser, while a separate `literalPool` held a copy — two copies causing cache misses on every clause-data access in propagation (the `clauseLits[0]` dereference was the single hottest line at 570ms). Fixed by making `literalPool` the single source of truth: `RebuildLiteralPool` now re-points `Clauses[i].Literals` into the pool after building it. Changed pool type from `[]uint32` to `[]Literal` for direct slicing. The previous two-copy attempt (reverted) caused 21 false-UNSATs from stale pool data after watch swaps; single-storage eliminates the root cause (swaps modify the pool directly, no sync needed). Profile: `clauseLits[0]` cache-miss cost 570ms→420ms (26%); 96e9379c 4.0s→3.8s. Soundness verified: minisat cross-check 0 mismatches, fuzzer 400/400. No suite regression (61/72).
- **Structure classifier: long-clause signal**: `analyzeInstanceStructure` scored structured instances primarily on binary-clause ratio (weight 0.6), misclassifying structured instances with many long (>3 literal) clauses but few binary clauses as random (StructuredScore 0.40 < 0.7). This sent them down the random path (`restartBase=5`, Glucose disabled, aggressive decay), causing catastrophic thrashing: 381 restarts in 6700 conflicts, 2-3 glue clauses learned, props/dec ~100, level 90-110. Fixed by adding `LongClauseRatio` and using `max(binaryScore, longScore)` as the size score — random k-SAT has 0% long clauses so only structured instances are reclassified (822378be/b8143c9d: 0.40→0.91). Search quality fixed (props/dec 100→5) but instances still timeout due to propagation throughput (Mode 2, separate issue). No suite regression (61/72, identical instance set). Soundness verified: fuzzer 400/400.
- **Phase saving fix**: `savedPhase` was initialized to `true` and never updated — the only write site (`solver_cdcl.go:2953`) was a no-op writing back the value just read, and neither `assignLiteral` (decisions) nor `assignLiteralByClause` (propagations) recorded assigned polarities. Result: every decision used `savedPhase[varIdx] = true` (negated literal → variable=false) forever; phase saving did nothing. Fixed by adding `s.savedPhase[varIdx] = lit.IsNegated()` in both assignment functions and removing the dead decision-site write. Now propagations record the polarity clauses force and decisions reuse them — standard CDCL phase saving. 57/72 → 61/72 (+4). Soundness verified: minisat cross-check 0 mismatches, fuzzer 200/200 sound.
- **Vivification throttling + deterministic budget**: Vivify was thrashing on small/fast-restart instances — `runVivification` fired every 50th Luby restart (line 2014), but on tiny instances Luby restarts trigger every few hundred conflicts, so vivify ran 51 times in 32s (47% of CPU) for negligible yield (6/585 clauses modified per round). Two fixes: (1) added a `vivifyMinConflictGap` gate (default 20000) so vivify only fires after ≥20K new conflicts, preventing thrashing on easy instances; (2) replaced the 500ms wall-clock time budget (nondeterministic — wall-clock truncation checked different clauses per run, perturbing the search trajectory) with a deterministic `maxCheckPerRound = 2000` clause-count cap. Result: `274099073` (80v) TIMEOUT→SAT@27s; 55/72 → 57/72 (+2 net). CLI: `-vivify-min-gap N` (0=restart-based only).
- **Tautology + duplicate-literal removal + pure literal elimination**: New preprocessing passes in `preprocessAggressive()` (before the structure gate). `removeTautologiesAndDuplicates()` scans all clauses, removes tautologies and dedups repeated literals in O(total literals) with a temporary seen-array. `pureLiteralElimination()` assigns single-polarity variables and removes satisfied clauses. Both run on ALL instances (trivially sound; don't change search trajectory the way forced unit propagation does). 54/72 → 55/72 (+2 gained: `66e6fea6` TIMEOUT→UNSAT, `b40a1e31` TIMEOUT→SAT; -1 lost: `8d58ca18` SAT@26.7s→TIMEOUT, boundary).
- **Assignment packing**: Packed `Assignment` to 8 bytes (`Level int32` + `Value bool`), eliminated separate `varLevel []int` cache. Every variable lookup now hits one 8-byte struct instead of two arrays on different cache lines. Memory for 290K vars: 5.7MB → 2.3MB.
- **LearnedClauseLoc packing**: Packed `learnedOffsets` + `learnedSizes` (two `[]int` = 16B across two cache lines) into single `[]LearnedClauseLoc` (`Offset int32` + `Size int32` = 8B). All 60 access sites use the same index for both fields (verified — no mismatched-index co-access). Also removed dead `ClauseMetadata.Offset`/`.Size` fields (never read; `Size` was set once but unused). `ClauseMetadata`: 72B → 16B (only `LBD int` + `PropCount int` remain; the other fields were removed as dead computation).
- **CDCL backjump** (`backtrack()`): Was using `trailHead[bjLevel]` (start of level bjLevel) as decision point, which unassigned the decision at bjLevel and re-assigned it FLIPPED (DPLL chronological backtracking). Fixed to `trailHead[bjLevel+1]` (keep level bjLevel, let `propagateAssertingLiteral()` handle the UIP). Matches `cancelUntil()`.
- **LBD storage filter**: `learnClause()` was discarding clauses with LBD > 8, meaning the solver did conflict analysis and threw away the result. Removed filter — store ALL learned clauses, use LBD for deletion priority only.
- **decide() simplification**: Removed non-standard diversification logic (random decisions, decidedVarSet override, consecutiveFlips tracking) that was overriding VSIDS. Standard CDCL: VSIDS + phase saving only.
- **VSIDS heap slice caching (A1)**: `vsidsHeap.down()` and `up()` accessed elements via `(*h)[i]` — pointer-to-slice indirection that the compiler can't optimize (can't prove the slice isn't reallocated across method calls). Profile showed `down()` at 14.5% of CPU, with the `(*h)[i]` dereference alone costing 280ms out of 620ms. Cached `s := *h` at the top of both functions (same pattern as `propagateWatched`'s `assignments`/`watchLists` caches). Sound because `down()`/`up()` never grow or reslice — they only swap elements in the backing array, which is shared between `s` and `*h`. Benchmark: 3241→3030 ns/op (6.5% speedup). No suite change (65/72). Soundness verified: fuzzer 300/300, cross-check 0 mismatches.
- **BIG BFS queue capacity drain fix (A2)**: `bigReachableInClause` used `q = q[1:]` to consume from the front, which moves the slice start forward and drains capacity. After a few cycles, `append` grew a new backing array (`mallocgc`→`growslice` was 6.1% of CPU). Fixed by using a head index (`q[head]; head++`) instead of slicing, keeping `q` anchored at `s.bigBfsQueue[:0]`. Eliminates all BFS queue allocations: `mallocgc` dropped out of the top 20 entirely, `bigReachableInClause` from 3.74% → 1.14%, allocs/op from 4375 → 3270 (25% reduction). No suite change (65/72, PAR2 7.26s → 7.20s). Soundness verified: fuzzer 300/300, cross-check 0 mismatches.
- **BIG-based clause minimization + dense-binary polarity gate**: (1) Sound transitive BIG minimization: `bigReachableInClause(lit)` does forward BFS from `lit` through `bigAdj` (binary implication graph built from binary clauses). A literal is removable iff BFS reaches another literal genuinely in the clause (matching polarity). Budget-limited (`maxBigBfsVisited=16`). Uses epoch-stamped `bigBfsVisited` array (not a set cleared per-call — the previous set-based approach was unsound: `tmpLitCovered` wasn't cleared on recursive removal → 3 false UNSAT). (2) Dense-binary polarity gate: git bisect found commit `b5c73fa` (polarity-based initial phase) caused `32baec6a` decision explosion (142→23369). The discriminator is density: dense binary instances (binaryRatio>0.9 AND density>35) have highly connected BIGs where the default phase propagates to a solution in 0 conflicts; the occurrence-based override fights the implication structure. Sparse binary instances (density<35) benefit from the override. `skipPolarityPhase` flag cached on solver in `getAdaptivePreprocessingConfig()`; `initVSIDSOccurrenceBonus()` gates the polarity override. 32baec6a: 23369→142 decisions (0 conflicts). Baseline (no BIG): 64/72, PAR2 7.96s → BIG (budget=8): 65/72, PAR2 7.22s (gain: bb34f22f TIMEOUT→UNSAT@~16s). Soundness verified: fuzzer 300/300, cross-check 0 mismatches.
- **BIG minimization soundness fix (tmpSeenVar clearing)**: BIG-removed literals left `tmpSeenVar[v]=true`, which recursive minimization interprets as "covered" (removable, can be skipped in reason clauses). But BIG removal is only valid while the BFS target remains in the clause — if recursive later removes the target, the BIG removal becomes invalid. This created a **circular dependency**: lit1 "covered" by BIG (depends on lit2 in clause), lit2 removed by recursive (treats lit1 as "covered"). The circular reasoning produced unsound clause minimization → false UNSAT on instance `028d0cc7` (SAT reported as UNSAT). Fix: clear `tmpSeenVar[v]=false` when BIG removes a literal, forcing recursive to re-examine v independently. Soundness verified: fuzzer 2000/2000, cross-check 0 mismatches.
- **C1: Periodic stats printing**: Added `-stats N` CLI flag that prints stats every N conflicts directly to stderr, bypassing the verbose-gated `s.Log` (which has 20-30% overhead from `interface{}` arg boxing at every `s.Log` call site plus `runtime.ReadMemStats` STW calls). The new `printPeriodicStats()` writes via `fmt.Fprintf(os.Stderr, ...)` — no arg boxing on hot paths, no ReadMemStats. Reports: conflicts, level, decisions, props, props/dec, learned count, emaLBD, avgLBD, cumulative avgLBD, minimization rate, BIG hit rate. Enables data-driven diagnosis of timeout instances without verbose overhead. Pure instrumentation — no behavior change. Soundness verified: cross-check 0 mismatches.
- **B3: BIG BFS budget increase (8→16)**: `maxBigBfsVisited` was 8 (function-local const in `bigReachableInClause`). Increasing to 16 finds more BIG-removable literals (soundness-preserving — the budget only reduces effectiveness, never soundness). On `028d0cc7`: BIG hit rate unchanged (~12%), minimization rate improved slightly. No suite change (65/72). PAR2: 7.53s → 7.01s (7% improvement — the larger budget produces more short clauses which propagate faster). Soundness verified: fuzzer 2000/2000, cross-check 0 mismatches.
- **B1: Level-capped restart for long-clause instances**: On long-clause instances (>80% long clauses), the Glucose EMA criterion never fires (high-LBD clauses produced consistently → EMA ≈ avg → `emaLBD > avg*ratio` never trips). The search goes deep (level 70+ on 822378be), producing high-LBD clauses with no propagation guidance — a deep-search → high-LBD → no-guidance → deep-search cycle. Added a 4th restart trigger in `shouldRestart()`: when `lastConflictLevel > restartLevelCap` (default 40) on long-clause instances (`longClauseRatio > 0.8`), force a restart. `lastConflictLevel` captured in `handleConflict()` (before `backtrack()` resets `s.level`). `longClauseRatio` cached in `getAdaptivePreprocessingConfig()`. Anti-thrashing gating mirrors props/dec restart: `lubyIndex >= 3`, `decisions > 10`, `levelRestartGap` (default 100) min-conflict gap. CLI: `-restart-level-cap N` (0=disabled), `-restart-level-gap N`. 822378be: level-capped restarts fire (level 55-88), but instance still TIMEOUT (throughput-bound). No suite change (65/72). PAR2: 7.02s. Soundness verified: fuzzer 2000/2000, cross-check 0 mismatches.
- **B2: Smaller clause DB for high-LBD instances**: When cumulative average learned clause LBD is consistently high (>10), shrinks `maxLearned` floor from `numVars*10` to `numVars*3` (one-way, no grow-back). Triggers deletion earlier, keeping only the lowest-LBD clauses. Uses a new cumulative LBD accumulator (`totalLbdSum`/`totalLbdCount`) that is NOT reset on restart (unlike `lbdSum`/`lbdCount` which are per-restart-window stats). The shrink fires once (when `totalLbdCount > 1000` and `totalLbdSum/totalLbdCount > 10`), then `maxLearnedShrunk=true` prevents re-evaluation. `dynamicLimit` is recomputed from `s.maxLearned` on every conflict, so the smaller limit takes effect immediately. On 822378be: shrink fires at ~1500 conflicts (cumulative avgLBD=14.8 > 10). No suite change (65/72). PAR2: 7.02s. Soundness verified: fuzzer 2000/2000, cross-check 0 mismatches.

### Potential Next Steps (July 2026)

Profiled on `BenchmarkRealInstanceLogAlloc` (post-A1+A2). CPU breakdown: `propagateWatched` 33.6%, `vsidsHeap.down` 13.2%, `learnClause` 11.9%, `backtrack` 7.1%, `onUnassign` 5.3%, `minimizeLearnedClause` 4.6%. `mallocgc` eliminated from top 20 (was 6.1% pre-A2).

All 6 timeout instances are solvable by minisat in <30s. Root causes: throughput (Go vs C, ~5-10x slower) and search quality (deep search → high-LBD clauses → no propagation guidance → deep search cycle).

#### A. Throughput Quick Wins (low risk, guaranteed speedup)

- **A3. Skip watch-list write-back for deletion-only path** (INFEASIBLE): Investigated and found infeasible. `watchLists[watchIdx] = watchList` (line 2963) is needed because `watchList = watchList[:lastIdx]` (line 2962) changes the slice length from `lastIdx+1` to `lastIdx`. Without the write-back, future calls to `propagateWatched` would load the stale (longer) slice and re-process deleted watch entries. The swap-with-last deletion DOES change the slice header — the original plan's claim "in-place, header unchanged" was incorrect.

#### B. Search Quality (medium risk, high potential gains)

- **B4. Target/best phase saving**: Track the "best" phase per variable (phase that led to most propagations or shallowest conflict). Use best phase instead of last phase for some decisions. Kissat-style. General search improvement.

#### C. Throughput Deep Dives (higher effort)

- **C2. Propagation loop micro-optimization**: Investigate bounds check elimination via `unsafe` for the inner watch loop. Consider cache-line alignment of `Watch` structs. Potential 10-15% propagation speedup.
- **C3. Inprocessing: variable elimination for large instances**: Standard Davis-Putnam VE (sound per soundness rules — ban is on the "pos=1" definitional variant, not standard VE). Could reduce 29K vars / 708K clauses significantly on de2b584e. Risky to implement (previous subsumption was buggy), but high impact for large instances.

### Previous Attempts & Caveats
- **Contiguous literal pool (two-copy)**: An earlier attempt kept `literalPool` as a separate `[]uint32` copy alongside `Clauses[].Literals` (which pointed to per-clause parser allocations). Watch position swaps modified `Clauses[].Literals` but not the pool copy, causing stale data. The single-storage fix (`Clauses[].Literals` pointing into the pool, pool type `[]Literal`) eliminates the root cause — swaps modify the pool directly. Always run `benchmark/cross_check_minisat.sh` after propagation changes.
- **VSIDS var_inc (MiniSat-style O(1) decay)**: Originally banned because the first attempt caused a -3 suite regression (61→58): the float-rounding difference shifts the search trajectory. **Re-enabled by commit `02eee62`** with parameter compensation: the old attempt kept the same decay factors but changed the float arithmetic. The successful re-attempt compensates the factors for the 5x more frequent decay rate (decayInterval 5→1): `initialDecay: 0.90^(1/5) ≈ 0.9792`, `maxDecay: 0.999^(1/5) ≈ 0.9998`, `lbdBonusDecay: 0.999^(1/5) ≈ 0.9998`, `decayRampUp: 5000 × 5 = 25000`. This makes the effective per-conflict decay rate approximately identical. The code now uses O(1) decay (`varInc /= decayFactor`) in `vsids.go:decay()`. The old O(n) scan was 7% of CPU.

## Architecture

### Data Structures
- **Literal**: `uint32` (bit 31=sign, bits 0-30=variable index). Variables 0-based internally, 1-based in DIMACS.
- **Watch**: 8 bytes (`ClauseIdx int32` + `Blit uint32`). `ClauseIdx` bit encoding: bit 31=learned flag, bit 30=myPos (which watch position 0/1), bits 0-29=clause index. `Blit` = blocking literal cached for fast-path skip.
- **Implication array encoding** (`s.implication`): `>=0` original clause; `<=-5` learned (`-learnedIdx-5`); `-1` decision; `-2` unit-prop preprocess; `-3` pure-literal; `-4` reserved. The 4-slot offset frees `-1..-4` as sentinels so learned-clause decode can't misread preprocessing sentinels (was a soundness bug). `Watch.ClauseIdx` uses a separate encoding (`-learnedIdx-1`); `propagateWatched` translates watch→implication at the assign site.
- **Learned clauses**: Contiguous literal pool with packed `LearnedClauseLoc` (`Offset int32` + `Size int32`) replacing separate offset/size arrays. Deletion uses tombstones (`learnedLoc[i].Size=0`); `compactLearnedClauses()` reclaims gaps at the next restart (level 0) when tombstones exceed ~33% of capacity.

### Key Files
- `internal/solver/solver_cdcl.go`: CDCL solver (1-UIP, backjumping, restarts, deletion+compaction, vivification)
- `internal/solver/vsids.go`: VSIDS/LRB/CHB variable selection with incremental activity heap
- `internal/cnf/cnf.go`: Core types (Literal, Clause, CNF, Watch — 8 bytes, no WatchPos)
- `cmd/satience/main.go`: CLI; `cmd/fuzz/main.go`: Fuzzer

## Key Design Decisions

These capture the *why* behind choices that aren't obvious from the code.

### Conflict Analysis & Propagation
- **UIP at position 0**: Learned clauses are reordered so the UIP is at position 0 and a backjump-level literal at position 1 (MiniSat asserting-clause invariant). Positions 0/1 are watched directly. After backjump, `qhead=decisionPoint` skips re-processing the earlier trail; the asserting literal is propagated explicitly by `propagateAssertingLiteral()` (O(clause-size) scan) since the watched literals sit at trail positions < decisionPoint.
- **1-UIP resolved-variable fix**: Skip re-adding already-resolved variables during 1-UIP. Without this, `currentCount` inflates and the loop never converges; the old fallback (dropping literals) was unsound (false UNSAT on 3d937949).
- **Level-0 literal skip**: Don't add level-0 literals to learned clauses (always true, no propagation benefit).
- **VSIDS activity NOT reset on restart**: Standard MiniSat behavior preserves activity across restarts. Resets (0.3× or 0.8×) destroy search memory and cause regressions.

### Restart Policy
- Glucose-style EMA LBD > ratio×avg (EMA α=0.1 smooths spikes; single-LBD triggers spurious restarts). Disabled for random instances (Luby base=5 suffices); Luby fallback base=100.
- CLI defaults: `restart-base=100`, `restart-glucose-ratio=1.5`, `restart-glucose-min=10`.

### Clause Database
- `calculateMaxLearned`: ~15% of clauses, min `max(300, numVars*10)`, max 100K. Deletion triggered at 150% of dynamic limit (`maxLearned + conflicts/50`).
- Tombstone deletion + periodic compaction over array rebuild: 23× GC reduction. Compaction runs at level-0 restart (no learned clause in use as reason) when tombstones > 33%. Compaction rebuilds only learned-clause watches (original-clause watches are preserved — originals don't move during learned-clause compaction).
- `chooseWatchPositions()`: Shared non-false literal selection for original/learned watch setup and compaction (preserves watched-literal invariant).

### Preprocessing (Adaptive)
- **Tautology + duplicate-literal removal** (`removeTautologiesAndDuplicates`): Runs on ALL instances before the structure gate. O(total literals) with a temporary seen-array; removes tautological clauses and dedups repeated literals. Trivially sound.
- **Pure literal elimination** (`pureLiteralElimination`): Runs on ALL instances before the structure gate. Assigns variables appearing with one polarity only, removes satisfied clauses. Trivially sound (no clause contains the opposite polarity).
- StructuredScore ≥ 0.7: unit propagation enabled. Score < 0.7: unit propagation disabled (causes 76× more conflicts on random instances). Tautology/duplicate removal and PLE still run on all instances.

### VSIDS Heap
- Lazy heap with sink approach: `bumpLarge`/`bumpLBD` don't call `increaseKey` (O(1) bump). `selectVariableWithHeap` peeks at root; if assigned, sinks to -Inf (stays in heap, drops to bottom). `onUnassign` restores sunk entries' real scores and sifts up. `buildHeap` includes ALL variables (assigned at -Inf, unassigned at real score) — heap never empties, eliminating frequent O(n) rebuilds. Periodic refresh every 2000 conflicts fixes deeply stale entries. `restart()` forces a rebuild.
- O(1) decay via `varInc /= decayFactor` (MiniSat varInc trick). `decay()` does NOT touch the heap (relative ratios preserved). Rescale at `varInc > 1e100` sets `heapValid = false`.
- `numUnassigned` field: O(1) allAssigned/hasUnassigned.

## Soundness Bugs to Never Re-introduce

- **pos=1 variable elimination**: Removed (~800 lines). The "pos=1 elimination" variant assumes positive clauses are *definitions* of a variable and reconstructs the eliminated variable's value from that assumption. In PHP instances, positive clauses are *constraints*, not definitions → returned SAT instead of UNSAT on php_6p_5h_unsat. Standard Davis-Putnam VE (resolve all (x∨A)×(¬x∨B) pairs, reconstruct by trying x=true then x=false) is sound — the ban is on the definitional variant, not standard VE.
- **Buggy subset-check subsumption elimination**: Removed (296 lines). The clause-subsumption check was implemented incorrectly — it determined clause A was subsumed by B when it wasn't, removing necessary clauses → false SAT. Forward subsumption (literal subset check) is sound — the ban is on the buggy implementation, not the algorithm.
- **Pattern-matching equivalence detection**: Disabled. The detection used ad-hoc pattern matching on binary clause pairs and produced false equivalences (claimed a↔b when variables were not equivalent). SCC-based equivalence detection (Tarjan's SCC on the binary implication graph) is sound — the ban is on the pattern-matching variant, not SCC-based detection.
- **Blocked clause elimination (BCE)**: Disabled — was removing ALL clauses from arg_chain instances. Safe per-clause in isolation but unsound when applied aggressively (removes interacting constraints).
- **Unit-prop inprocessing at restart**: Removed (34-228% slowdown, no benefit).
- **Vivification**: Re-enabled after 3 fixes (stale `tmpLearnedLits` length, unit-scan false UNSAT via `inVivification` flag, watch moves not re-checked). Does NOT remove TRUE literals at Level > 0 (unsound).

## Critical Context

- Go 1.22+, GOAMD64=v3 for AVX2/BMI2
- CLI flag order: `-model file.cnf` works, `file.cnf -model` does not
- GBD download: `https://benchmark-database.de/file/<hash>`
- Fuzzer: `./fuzz -n 100 -mode random` (modes: random, structured, pigeonhole; verifies SAT models)
- Soundness eval: `benchmark/eval_small_random.sh [n]`
